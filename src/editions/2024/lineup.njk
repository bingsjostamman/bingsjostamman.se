---
layout: layouts/base
title: "Artister {{ year }}"
year: 2024
eleventyComputed:
  permalink: "{% if year == editions.current %}/scen/index.html{% else %}/{{ year }}/scen/index.html{% endif %}"
---

<h1>Artister {{ year }}</h1>

<ul>
{% for artist in lineup %}
  <li>
    <a href="{% if year == editions.current %}/scen/{{ artist.name | slugify }}/{% else %}/{{ year }}/scen/{{ artist.name | slugify }}/{% endif %}">
      {{ artist.name }}
    </a>
  </li>
{% endfor %}
</ul>